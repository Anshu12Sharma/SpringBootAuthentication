package com.learn;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;

@Controller
public class HomeController {
@GetMapping("/home")
    public String home(){
    return "this is home page";
}
    @GetMapping("/login")
    public String login(){
        return "DO LOGIN";
    }
    @GetMapping("/register")
    public String register(){
        return "this is registration page";
    }
}

>>controller>>@RestController>>Service>@Services(Call DB method or other buisness logic)
>>PoJo classes(model)>>DB(Dao, if use JPA then reporsitorey)
>>>>>>>>>>>>>>>>>>>>>>>>>>
git clone URL
git branch>>default( master)
git checkout -b local_branch
        git status
git commit -a(if all file)  -m "commit msg"
        else git add file(with path seen on terminal)
        git pull origin master --rebase
git checkout master
git merge local_branch
git status
git push origin master